[project]
name = "wesen"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "numpy==2.4.4",
    "pillow>=12.2.0",
    "pyopengl>=3.1.10",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/wesen"]
