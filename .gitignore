*.py[cod]
__pycache__
# C extensions
*.so

# Packages
*.egg
*.egg-info
dist
build
eggs
parts
bin
var
sdist
develop-eggs
.installed.cfg
lib
lib64

# Translations
*.mo

# Profiling
*.stats

# Screenshots: movie frames, and the numbered shots the `c` key keeps.
# screenshot.png is the one in the README and IS tracked - the GUI
# refreshes it on every click, so it stays a picture of the real game.
m*.png
wesen-*.png

#emacs backup
*~

#ropemacs
.ropeproject

debug.out
profile.stats_before

#Mac
.DS_Store

#fuer mein PyCharm
.idea/

# throwaway development scripts, runs and logs
local/

# uv.lock is deliberately NOT ignored. The same seed has to play the
# same game, and a game is played by numpy; a clone that resolves its
# own dependency versions is a clone that can disagree with this one.
